# dependencies & build output
node_modules/
app/node_modules/
app/release/
app/dist/

# local runtime data (created at runtime, never committed)
config.json
metrics.json
*.log

# licensing server secrets & data (NEVER commit the private key)
server/.keys/
server/.data/

# OS / editor
.DS_Store
Thumbs.db
.vscode/
.idea/
*.tmp
